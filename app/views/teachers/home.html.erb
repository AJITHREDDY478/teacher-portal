<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Keyframe animation for moving text */
        @keyframes move {
            0% {
                transform: translateY(-20px);
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
            100% {
                transform: translateY(0);
            }
        }

        /* Flashing effect */
        @keyframes flash {
            0%, 100% {
                opacity: 1;
            }
            50% {
                opacity: 0.5;
            }
        }

        /* Apply the animations */
        .move-animation {
            animation: move 1s ease-in-out forwards;
        }

        .flash-animation {
            animation: flash 1s ease-in-out infinite;
        }
    </style>
    <title>Teacher Portal</title>
</head>
<body class="bg-gray-100 flex flex-col min-h-screen">
    <!-- Main Content -->
    <div class="flex items-center justify-center flex-grow text-center">
        <div>
            <h1 class="text-4xl font-bold text-indigo-600 move-animation">Welcome to the Teacher Portal!</h1> <!-- Changed color here -->
            <p class="mt-4 text-lg text-gray-700 flash-animation">Please login!</p>
        </div>
    </div>
</body>
</html>
